How to use the app.

Run the command line application with the following parameters:
AgeAggregator.App.exe <inputDataDirectoryPath> <outputFilePath>

Examplary arguments which will work for sure due to committed files structure:
AgeAggregator.App.exe "..\..\..\Data" "..\..\..\Data\Output\output.json""